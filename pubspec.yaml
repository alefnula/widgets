name: widgets
description: Widgets and utilities.
version: 0.0.1
homepage: https://github.com/alefnula/widgets

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^1.0.3
  tray_manager: ^0.1.6
  window_manager: ^0.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
